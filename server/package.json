{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "root.ts",
  "scripts": {
    "start": "nodemon --watch '**/*.ts' --exec 'ts-node' -r tsconfig-paths/register root.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.0.3",
    "nodemon": "^2.0.16",
    "ts-node": "^10.8.1",
    "tsconfig-paths": "^4.0.0"
  }
}
